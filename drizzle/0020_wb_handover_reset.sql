-- Wildberries: сброс времени передачи в доставку, заполненного миграцией 0019.
--
-- У WB shipped_at переписывался при каждой синхронизации временем самой
-- синхронизации, поэтому скопированное из него время почти у всех заказов
-- оказалось «сегодня». Настоящее время — закрытие поставки (closedAt): его
-- проставит ближайшая синхронизация заказов за последние 30 дней.
UPDATE `orders` SET `handed_over_at` = NULL WHERE `marketplace_id` = 'wildberries';
